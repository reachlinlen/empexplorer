import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
  container: {
    maxWidth: '600px',
    marginLeft: 'auto',
    marginRight: 'auto',
  },
}))

export default useStyles